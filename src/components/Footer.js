import React from 'react';

const Footer = () => {
    return (<footer className="footer">
        <div className="container">
            <span className="text-muted">Contact Us for more Information...</span>
        </div>
    </footer>);
}

export default Footer;